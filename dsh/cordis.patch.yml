# Cymose graph — tools the model calls to branch, inherit, and close a node.
# Install: dsh plugin add github:cymosehq/cymose-code
# Token: web.cymose.app → Settings → Connected apps.
- insert:
    - id: cymose
      name: cymose-code/dsh
      config:
        apiUrl: https://api.cymose.app
        token: ""
